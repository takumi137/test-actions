# .github/workflows/test.yml
name: Test Fargate Runner

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  test:
    runs-on: [self-hosted, fargate]
    steps:
      - run: echo "Hello from Fargate-based runner"
